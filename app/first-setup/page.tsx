import { FirstTimeSetupWizard } from "@/components/first-time-setup-wizard"

export default function FirstSetupPage() {
  return (
    <div className="min-h-screen bg-gradient-to-br from-blue-50 to-indigo-100">
      <FirstTimeSetupWizard />
    </div>
  )
}
